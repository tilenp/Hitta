<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/reviewsFragment">

    <fragment
        android:id="@+id/reviewsFragment"
        android:name="com.example.hitta.reviews_screen.ReviewsFragment"
        android:label="@string/Reviews"
        tools:layout="@layout/fragment_reviews"
        >
        <action
            android:id="@+id/action_reviewsFragment_to_reviewFragment"
            app:destination="@id/reviewFragment" />
    </fragment>
    <fragment
        android:id="@+id/reviewFragment"
        android:name="com.example.hitta.review_screen.ReviewFragment"
        android:label="ReviewFragment"
        tools:layout="@layout/fragment_review"/>
</navigation>